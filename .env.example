# Environment Variables for Render Deployment
# Copy these settings to your Render Dashboard → Environment

# Flask Secret Key (REQUIRED)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-generate-a-secure-one

# Flask Environment (REQUIRED)
FLASK_ENV=production

# Python Version (REQUIRED)
PYTHON_VERSION=3.11.0

# Node Version (Optional - Render auto-detects)
NODE_VERSION=18.17.0

# Port (Optional - Render auto-assigns)
# PORT=10000

# CORS Allowed Origins (Update after deployment)
# Format: comma-separated URLs
# ALLOWED_ORIGINS=https://your-app.onrender.com,https://yourdomain.com

# Database Path (Optional - uses default)
# DB_PATH=checkpoints/movies.db

# Application Settings (Optional)
# MAX_WORKERS=4
# TIMEOUT=120
